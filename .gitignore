node_modules/
.next/

# env files — ignore all, but allow the example template
.env*
!.env.example

# secrets / keys
*.pem
*.key

*.log
.superpowers/

# Claude Code local config / worktrees
.claude/

# Playwright output (repo root)
/test-results/

# Separate tools living alongside the wiki (tracked elsewhere / not part of sand-wiki)
/sand-scraper/
/tech-tree/
/sek

# Press-kit key art: the source PNG/JPG are ~166MB and must not enter the repo.
# Only the small optimized webps under art/optimized/ are committed and served.
/apps/wiki/public/art/*
!/apps/wiki/public/art/optimized/